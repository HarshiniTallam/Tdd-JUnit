package com.epam.jdd_demo;

public class SwapLast2Chars {

	public String swap(String string) {
		int len = string.length();
		char secondlst = string.charAt(len - 1);
		char lst = string.charAt(len - 2);
		String result = string.substring(0, len - 3);
		return ""+ result +secondlst+lst;
	}
	
	

}
